# 第 15 课：模型选型与部署策略

> **核心问题**：云 API、私有化、开源模型怎么选？怎么部署？
> **预计时间**：2 天
> **前置知识**：第 13 课（企业架构）、第 14 课（成本经济）

---

## 一、选型的三个维度

```
选模型 = 在 质量、成本、合规 三角中找平衡

        质量（准不准、聪不聪明）
        /\
       /  \
      /    \
  成本      合规/安全
（贵不贵）  （数据出不出域）
```

**没有最好的模型，只有最适合场景的模型。**

---

## 二、三大部署模式对比

### 2.1 云 API（调用大厂服务）

```
优点：
  ✓ 质量最高（GPT-4o、Claude 等前沿模型）
  ✓ 零运维（不用管 GPU、模型升级）
  ✓ 按量付费（起步成本低）
  ✓ 开箱即用（SDK 成熟）

缺点：
  ✗ 数据出域（隐私合规问题！）
  ✗ 成本随量增长（没有上限）
  ✗ 依赖外部（供应商故障/涨价/停服）
  ✗ 无法定制（不能微调模型本身，部分支持）

适用：非敏感数据、对质量要求高、不想运维
```

### 2.2 私有化部署（自己跑开源模型）

```
优点：
  ✓ 数据不出域（合规安全）
  ✓ 成本封顶（硬件一次性投入）
  ✓ 完全可控（模型、服务、升级自己定）
  ✓ 可微调（用私有数据训练）

缺点：
  ✗ 需要 GPU 硬件（贵：一张 A100 约 ¥5-10 万）
  ✗ 需要运维（部署、监控、升级）
  ✗ 模型能力落后云 API 1-2 代
  ✗ 技术门槛高

适用：敏感数据（简历/医疗/金融）、长期大量调用
```

### 2.3 混合模式（推荐！）⭐

```
原则：敏感任务本地，非敏感任务云端

你项目的实践（记忆确认）：
  "Ollama + 云 API + 混合模式"
  本地 Ollama：Embedding（简历向量化，数据不出域）
  云 API：复杂分析、生成（质量优先）

混合路由示例：
  简历 Embedding → 本地 Ollama（数据不出域）
  简历分析 → 云 API（内容已脱敏）
  简单提取 → 本地小模型（省钱）
  复杂推理 → 云端强模型（质量）
```

---

## 三、主流模型能力地图（参考 2026 年）

### 3.1 对话/生成模型

| 梯队 | 模型 | 特点 | 适合 |
|------|------|------|------|
| 顶级 | GPT-4o、Claude 3.7、Gemini 2.5 | 综合最强 | 复杂推理、关键决策 |
| 优秀 | Qwen-Max、DeepSeek-V3、GLM-4 | 中文强、性价比高 | 日常业务分析 |
| 均衡 | GPT-4o-mini、Qwen-Turbo、DeepSeek-V3-Lite | 便宜够用 | 提取、分类、格式化 |
| 开源 | Qwen2.5-72B、Llama-3.1-70B、DeepSeek-R1 | 可私有化 | 数据敏感场景 |
| 本地 | Qwen2.5-7B、Llama-3.2-8B、Gemma-2-9B | 免费、快 | 简单任务、Embedding |

### 3.2 Embedding 模型

| 模型 | 维度 | 特点 |
|------|------|------|
| BGE-M3 | 1024 | 中文最强，多语言，可本地部署 |
| text-embedding-3 | 1536/3072 | OpenAI，质量高 |
| text-embedding-ada-002 | 1536 | 经典 |
| nomic-embed | 768 | 开源可本地 |
| Ollama + bge-m3 | 1024 | 你项目可选，数据不出域 |

### 3.3 重排序模型（Rerank）

```
BGE-Reranker-v2-m3：中文场景首选
Cohere Rerank：云端
本地部署 0.5B 模型即可（轻量）

你的场景：简历检索 + Rerank 效果提升明显
```

---

## 四、私有化部署实战

### 4.1 硬件需求估算

```
估算原则：参数量 × 精度字节数 = 显存需求

7B 模型 FP16（16bit）→ 约 14GB 显存 → RTX 4090(24G) 可跑
8B 模型量化 INT4 → 约 5-6GB → 消费级显卡可跑
72B 模型 FP16 → 约 144GB → 需要 2×A100(80G) 或更多
量化（GGUF/INT4）能省 4 倍显存，质量损失约 1-3%

Embedding 模型（0.5B）→ 2GB 显存 → 随便一台机器
```

### 4.2 部署工具选型

| 工具 | 特点 | 适合 |
|------|------|------|
| **Ollama** | 一键部署，CPU/GPU 都行 | 开发/小规模（你项目在用）|
| **vLLM** | 高性能推理，吞吐高 | 生产大规模 |
| **llama.cpp** | 量化运行，内存友好 | 资源受限环境 |
| **TGI** | HuggingFace 出品 | 生产部署 |
| **SGLang** | 新秀，性能强 | 生产大规模 |

```
你项目的演进路径：
  Ollama（开发调试）→ 数据量大后换 vLLM（生产）
  两者都支持 OpenAI 兼容 API → 代码不用改！
```

### 4.3 vLLM 部署示例

```bash
# 安装（需要 Python 3.9+ 和 GPU）
pip install vllm

# 启动服务（OpenAI 兼容 API）
vllm serve Qwen/Qwen2.5-72B-Instruct \
  --port 8000 \
  --max-model-len 32768 \
  --gpu-memory-utilization 0.9

# 调用方式和 OpenAI API 一样
curl http://localhost:8000/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model": "Qwen/Qwen2.5-72B-Instruct", "messages": [{"role": "user", "content": "你好"}]}'
```

---

## 五、选型决策矩阵（实用工具）

```
对每个 AI 功能，按此表打分决策：

┌──────────────────────────────────────────────────┐
│ 功能：简历分析                                     │
│                                                  │
│ 数据敏感度：中（脱敏后可出域）                       │
│ 质量要求：高（错误分析会影响招聘决策）               │
│ 调用量：每天 1000 次（量大）                        │
│ 延迟要求：中（可接受 10s）                         │
│ 成本预算：¥3000/月                                 │
│                                                  │
│ 结论：云 API 优秀模型 + 本地 Ollama 预提取          │
│       混合模式，单次成本控制在 ¥0.02 以内            │
└──────────────────────────────────────────────────┘

评估维度：
  1. 数据敏感度（高 → 本地）
  2. 质量要求（高 → 云端强模型）
  3. 调用量（大 → 分级 + 本地）
  4. 延迟要求（低延迟 → 本地/快模型）
  5. 预算（有限 → 便宜模型 + 缓存）
  6. 合规约束（强制本地 → 私有化）
```

---

## 六、模型评测（选型依据）

### 6.1 评测方法

```
1. 公开基准：MMLU（知识）、HumanEval（代码）、C-Eval（中文）
   参考价值：快速了解模型能力等级

2. 领域测试集：用你的真实数据测试 ⭐ 最重要！
   构建 30-50 条典型任务 → 候选模型各跑一遍
   人工打分：准确性、格式、稳定性

3. A/B 对比：小流量线上对比
   新模型 vs 旧模型 → 看指标差异

4. 成本测试：同样的任务，实测 token 消耗
   不同模型对同一任务的 token 效率差异很大
```

### 6.2 评测清单（你的 HR 场景）

```
候选模型对比表：
  模型        简历提取准确率  匹配评分合理性  平均延迟  单次成本
  GPT-4o       98%           92%          8s      ¥0.15
  Qwen-Max     97%           90%          5s      ¥0.03
  DeepSeek     96%           88%          6s      ¥0.01
  Ollama-7B    85%           70%          2s      免费

结论：性价比 → DeepSeek；质量 → GPT-4o；敏感 → Ollama
```

---

## 七、模型升级与迁移

```
模型是快速演进的，架构上必须"模型无关"：

1. 统一接口：Spring AI / LangChain4j 封装（你已在用）
2. 配置化：模型名、地址、密钥全部配置化
3. 不绑定特性：不依赖某模型特有功能（如某模型的特殊 tool 格式）
4. 评估先行：升级前先跑黄金数据集

你项目的实践：Spring AI 抽象层 → 换模型只改配置
这是"模型无关"架构的最佳实践
```

---

## 八、本课小结

```
核心要点：
1. 三种部署模式：云 API（质量）、私有化（合规）、混合（推荐）
2. 混合模式：敏感数据本地 + 非敏感云端
3. 硬件估算：7B FP16 ≈ 14GB 显存，量化省 4 倍
4. 工具选型：Ollama（开发）→ vLLM（生产）
5. 选型决策：数据敏感度/质量/量/延迟/预算/合规 六维评估
6. 评测：领域测试集 > 公开基准，实测 token 成本
7. 架构必须模型无关（Spring AI 抽象层）
```

---

## 九、思考题

1. **你们项目的简历数据适合送云端模型吗？哪些字段必须脱敏？**
2. **如果简历量涨到每天 1 万份，Ollama 还够用吗？换 vLLM 的理由是什么？**
3. **"质量要求高"和"预算有限"冲突时，你的取舍方案是什么？**
4. **为什么说"架构模型无关"是企业 AI 的生存法则？**

---

## 十、实战练习

1. 用 C-Eval/MMLU 排行榜对比 3 个你感兴趣的模型
2. 用你项目 10 条真实简历，对比本地 Ollama 和云端模型的提取质量
3. 为你的 AI 面试功能填写"选型决策矩阵"
4. 如果公司要求简历数据不出域，设计一份模型方案（硬件+模型+成本）
